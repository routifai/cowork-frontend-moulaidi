# Context Map

## Contexts

- [Presenting](./presenting/CONTEXT.md) — the Hypatia PowerPoint Builder's frontend: sidebar entry, editor UI, ported from the open-source Presenton project

No root `CONTEXT.md` exists yet — this repo has no other documented bounded context so far. `Presenting` is scoped to its own folder deliberately, not the root, so it stays deletable as one unit.
