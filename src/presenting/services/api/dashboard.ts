/** Stub for DashboardApi — Presenton's HTTP dashboard API. */
export const DashboardApi = {
	getPresentation: async (_id: string, _opts?: unknown) => {
		throw new Error("Use Tauri presenting engine (presenting_get_presentation)");
	},
};
