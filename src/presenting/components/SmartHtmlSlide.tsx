/** Stub — Smart HTML slide renderer. */

interface SmartHtmlSlideProps {
	slide?: unknown;
	[key: string]: unknown;
}

export default function SmartHtmlSlide(_props: SmartHtmlSlideProps) {
	return <div />;
}
