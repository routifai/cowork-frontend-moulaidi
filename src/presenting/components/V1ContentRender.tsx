/** Stub — V1 slide content renderer. */

export interface V1ContentRenderProps {
	slide?: unknown;
	[key: string]: unknown;
}

export function V1ContentRender(_props: V1ContentRenderProps) {
	return <div />;
}
